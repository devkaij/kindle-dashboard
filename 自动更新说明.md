# Kindle Dashboard 自动更新方案

## 📱 离线后能否更新数据？

**答案：取决于使用方案**

---

## 方案对比

| 方案 | 离线可用 | 自动更新 | 需要电脑 |
|------|---------|---------|---------|
| **网页版** | ❌ 需要网络 | ✅ 自动 | ❌ 不需要 |
| **静态文件** | ✅ 离线可读 | ❌ 需手动生成 | ⚠️ 生成时需要 |
| **GitHub Actions** | ❌ 需要网络 | ✅ 自动 | ❌ 不需要 |

---

## 推荐方案

### 方案 A：网页版（最简单）
```
https://devkaij.github.io/kindle-dashboard/
```
- Kindle 浏览器直接访问
- 点击屏幕手动刷新
- 数据实时自动更新
- **缺点**：需要 Kindle 联网

### 方案 B：GitHub Actions 自动更新
- 仓库配置了定时任务
- 每天 8:00/12:00/18:00/22:00 自动生成新文件
- 可搭配网页版使用

### 方案 C：电脑定时任务（需要电脑开机）

#### Windows 步骤：
1. 打开「任务计划程序」
2. 创建基本任务 → 输入名称 "Kindle Dashboard 更新"
3. 触发器：每天 8:00、12:00、18:00
4. 操作：启动程序 `auto_update.bat`
5. 勾选「使用最高权限运行」

#### 或手动运行：
```bash
cd D:\AI Desktop\kindle-dashboard
python auto_update.py
```

---

## 各方案优缺点

### 网页版（推荐）
```
✅ 优点：
   - 无需电脑运行
   - 数据实时自动更新
   - 点击即可刷新
   
❌ 缺点：
   - 需要 Kindle 联网
   - 墨水屏刷新有残影
```

### 静态文件
```
✅ 优点：
   - 离线可查看
   - 无残影问题
   
❌ 缺点：
   - 数据固定，不会自动更新
   - 需手动重新生成发送
```

### GitHub Actions
```
✅ 优点：
   - 完全自动化
   - 无需电脑
   
❌ 缺点：
   - 需要网络才能生效
   - 网页版仍有 CDN 延迟
```

---

## 💡 最佳实践

**组合使用：**

1. **日常使用**：打开网页版 https://devkaij.github.io/kindle-dashboard/
2. **离线备份**：定期运行 `generate.py` 生成静态文件保存到 Kindle
3. **自动更新**：配置 Windows 计划任务或依赖 GitHub Actions

---

## ⏰ 设置定时任务（Windows）

### 方法一：任务计划程序（推荐）
1. Win + R → 输入 `taskschd.msc`
2. 右侧点击「创建基本任务」
3. 名称：`Kindle Dashboard 更新`
4. 触发器：每天多次（如 8:00, 12:00, 18:00）
5. 操作：启动程序
   - 程序：`python.exe`
   - 参数：`"D:\AI Desktop\kindle-dashboard\auto_update.py"`
   - 起始位置：`D:\AI Desktop\kindle-dashboard`
6. 勾选「打开属性对话框」
7. 勾选「使用最高权限运行」
8. 完成

### 方法二：开机自启
将快捷方式放入启动文件夹：
```
Win + R → shell:startup
```
创建快捷方式指向 `auto_update.bat`

---

## 🔧 GitHub Actions 状态

当前已配置自动更新：
- **触发时间**：每天 8:00/12:00/18:00/22:00 (UTC+8)
- **操作**：自动生成最新 HTML 文件并推送

查看运行状态：
```
https://github.com/devkaij/kindle-dashboard/actions
```

---

**总结**：离线状态下无法自动更新，但可以通过网页版实时查看（需联网），或提前生成多个静态文件备用。
